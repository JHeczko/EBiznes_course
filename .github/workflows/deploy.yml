name: CI/CD Build and Push

on:
  push:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  # GitHub automatycznie zamieni jheczko/nazwa-repo na małe litery
  BACKEND_IMAGE: ghcr.io/${{ github.repository }}-backend:latest
  FRONTEND_IMAGE: ghcr.io/${{ github.repository }}-frontend:latest

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./zadanie10/backend
          file: ./zadanie10/backend/Dockerfile
          push: true
          tags: ${{ env.BACKEND_IMAGE }}

      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./zadanie10/frontend
          file: ./zadanie10/frontend/Dockerfile
          push: true
          tags: ${{ env.FRONTEND_IMAGE }}